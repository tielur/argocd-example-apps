apiVersion: batch/v1
kind: CronJob
metadata:
  name: greetings
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - args:
            - Hello
            command:
            - echo
            image: alpine:latest
            name: main
  schedule: */2 * * * *